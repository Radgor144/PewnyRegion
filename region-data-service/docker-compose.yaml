services:
  postgres:
    image: cgr.dev/chainguard/postgres:latest
    container_name: regions-postgres
    environment:
      POSTGRES_DB: regions
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  region-data-service:
    container_name: region-data-service
    image: region-data-service:latest
    build:
      context: ..
      dockerfile: /Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    depends_on:
      - postgres
