services:
  postgres:
    image: postgres:16
    container_name: regions-postgres
    environment:
      POSTGRES_DB: regions
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
